[['threshold_low, threshold_high]` range\n- Among qualifying pixels, select the one with maximum brightness\n- Replace current pixel with the selected pixel\'s color\n\n### Parameters\n\n| Parameter | WGSL Uniform | Range | Default | Audio Source | Description |\n|-----------|--------------|-------|---------|--------------|-------------|\n| `radius` | `u_radius` | 1–100 | 10 | None | Horizontal search radius in pixels. Larger values = wider sorting windows. |\n| `threshold_low` | `u_threshold_low` | 0.0–1.0 | 0.2 | None | Minimum brightness for sortable pixels (0 = black, 1 = white). |\n| `threshold_high` | `u_threshold_high` | 0.0–1.0 | 0.8 | None | Maximum brightness for sortable pixels. |\n| `direction` | `u_direction` | -1/1 | 1 | None | Sort direction: 1 = rightward search, -1 = leftward search. |\n| `feedback_amount` | `u_feedback` | 0.0–1.0 | 0.0 | None | Additional feedback mix into final blend factor. |\n| `u_mix` | `u_mix` | 0.0–1.0 | 1.0 | None | Overall blend with original video (1.0 = full effect, 0.0 = bypass). |\n\n### Parameter Interactions\n\n- **radius + thresholds**: Large radius with narrow thresholds may find no qualifying pixels, resulting in minimal effect. Small radius with wide thresholds creates localized sorting.\n- **threshold_low/high**: Setting both to 0.5 creates mid-tone sorting only. Setting low=0, high=1 sorts all pixels (most aggressive).\n- **direction**: Rightward search (1) processes left-to-right; leftward (-1) processes right-to-left. Can create different flow patterns.\n- **feedback_amount**: Adds recursive feedback; can cause cumulative sorting effects if applied repeatedly.\n\n---\n\n## Public Interface\n\n### Class: `PixelSortEffect`\n\n**Inherits from:** `Effect` (base class for all VJLive3 effects)\n\n**Metadata:**\n```python\nMETADATA = {\n    "spec": "P3-EXT127', 'tier": "Pro-Tier Native', 'version": "1.0.0', 'category": "datamosh', 'tags": ["sort', 'glitch', 'brightness', 'neighborhood'], ['str, Any]`  \n  Serializes current parameter values.\n\n**Uniforms Set:**\n- `u_radius` (int, cast from float)\n- `u_threshold_low` (float)\n- `u_threshold_high` (float)\n- `u_direction` (int, -1 or 1)\n- `u_feedback` (float)\n- `u_mix` (float)\n- `u_time` (float, inherited)\n- `u_resolution` (vec2<f32>, inherited)\n\n---\n\n## Inputs and Outputs\n\n### Inputs\n\n| Input | Type | Description |\n|-------|------|-------------|\n| `tex0` | `texture_2d<f32>` | Current video frame (source) |\n| `samp` | `sampler` | Linear filtering sampler |\n\n### Outputs\n\n- **Fragment shader output** (`fragColor`): RGBA color with sorted pixel replacement\n- No feedback loop required (unlike PixelBloomEffect)\n\n### Data Flow\n\n```\nfor each pixel (x,y):\n  examine horizontal window [x-radius, x+radius]\n  for each pixel in window:\n    if brightness in [threshold_low, threshold_high]:\n      if brightness > current_max:\n        current_max = brightness\n        selected_pixel = pixel_color\n  output = selected_pixel\nfinal = mix(original, output, u_mix)\n```\n\n---\n\n## Edge Cases and Error Handling\n\n### Edge Cases\n\n1. **Radius exceeds image bounds**  \n   - At image edges, clamp search window to valid UV range [0, 1]  \n   - No error; gracefully handle boundary conditions\n\n2. **Thresholds inverted (low > high)**  \n   - Clamp `threshold_low` to ≤ `threshold_high`  \n   - Log warning if detected\n\n3. **No qualifying pixels found**  \n   - If no pixels in window meet threshold criteria, return original pixel  \n   - Not an error; expected behavior for selective sorting\n\n4. **Radius = 0**  \n   - Window size = 1 pixel; effect disabled (output = input)  \n   - Valid but minimal effect\n\n5. **Extremely large radius**  \n   - May cause performance issues due to many texture samples  \n   - Clamp to reasonable maximum (e.g., 200 pixels) with warning\n\n6. **Equal thresholds**  \n   - If `threshold_low = threshold_high`, only exact brightness values qualify  \n   - May result in sparse sorting; valid behavior\n\n### Error Handling\n\n- **Shader compilation failure**: Fallback to passthrough shader; log error\n- **Invalid uniform types**: Catch and log; continue with defaults\n- **Texture sampling errors**: Clamp UV coordinates to [0, 1]; no NaN propagation\n\n---\n\n## Mathematical Formulations\n\n### Brightness Calculation\n\nStandard luminance formula:\n```\nbrightness = dot(color.rgb, vec3(0.299, 0.587, 0.114))\n```\n\nAlternative (simpler):\n```\nbrightness = max(max(color.r, color.g), color.b)  // intensity\n```\n\n### Neighborhood Search\n\nFor pixel at UV `(u, v)`:\n```\n// Convert UV to pixel coordinates\npixel_x = u * resolution.x\npixel_y = v * resolution.y\n\n// Define search window\nstart_x = max(0, pixel_x - radius)\nend_x = min(resolution.x - 1, pixel_x + radius)\n\nbest_brightness = -1.0\nbest_color = current_color\n\nfor x in start_x to end_x:\n    sample_uv = vec2(x / resolution.x, v)\n    sample_color = texture(tex0, sample_uv)\n    sample_brightness = dot(sample_color.rgb, vec3(0.299, 0.587, 0.114))\n    \n    if (sample_brightness >= threshold_low && sample_brightness <= threshold_high):\n        if (sample_brightness > best_brightness):\n            best_brightness = sample_brightness\n            best_color = sample_color\n\nfragColor = mix(current_color, best_color, u_mix)\n```\n\n### Directional Search\n\n```\nif direction == 1:  // rightward\n    for x in pixel_x to min(pixel_x + radius, resolution.x - 1):\n        // search right\nelse:  // leftward\n    for x in max(pixel_x - radius, 0) to pixel_x:\n        // search left\n```\n\n### Full WGSL Fragment (simplified)\n\n```wgsl\n@group(0) @binding(0) var tex0: texture_2d<f32>;\n@group(0) @binding(1) var samp: sampler;\n\nstruct Uniforms {\n    u_time: f32,\n    u_resolution: vec2<f32>,\n    u_mix: f32,\n    u_radius: f32,\n    u_threshold_low: f32,\n    u_threshold_high: f32,\n    u_direction: f32,  // -1.0 or 1.0\n    u_feedback: f32,\n};\n\nvar<uniform> uniforms: Uniforms;\n\n@fragment\nfn fs_main(@location(0) uv: vec2<f32>) -> @location(0) vec4<f32> {\n    let current = textureSample(tex0, samp, uv);\n    let current_brightness = dot(current.rgb, vec3<f32>(0.299, 0.587, 0.114));\n    \n    var best_color = current;\n    var best_brightness = -1.0;\n    \n    // Convert UV to pixel coordinates\n    let pixel_x = uv.x * uniforms.u_resolution.x;\n    let pixel_y = uv.y * uniforms.u_resolution.y;\n    \n    // Define search bounds\n    let radius_int = i32(uniforms.u_radius);\n    var start_x = i32(pixel_x);\n    var end_x = i32(pixel_x);\n    \n    if (uniforms.u_direction > 0.0) {\n        end_x = min(i32(uniforms.u_resolution.x) - 1, start_x + radius_int);\n    } else {\n        start_x = max(0, start_x - radius_int);\n    }\n    \n    // Search neighborhood\n    for (var x: i32 = start_x; x <= end_x; x = x + 1) {\n        let sample_uv = vec2<f32>(f32(x) / uniforms.u_resolution.x, uv.y);\n        let sample_color = textureSample(tex0, samp, sample_uv);\n        let sample_brightness = dot(sample_color.rgb, vec3<f32>(0.299, 0.587, 0.114));\n        \n        if (sample_brightness >= uniforms.u_threshold_low && \n            sample_brightness <= uniforms.u_threshold_high) {\n            if (sample_brightness > best_brightness) {\n                best_brightness = sample_brightness;\n                best_color = sample_color;\n            }\n        }\n    }\n    \n    // Blend with feedback\n    let blend_factor = clamp(uniforms.u_mix + uniforms.u_feedback, 0.0, 1.0);\n    let result = mix(current, best_color, blend_factor);\n    return result;\n}\n```\n\n---\n\n## Performance Characteristics\n\n### Computational Complexity\n\n- **Texture samples**: Up to `2 * radius + 1` per fragment (worst case)\n- **Arithmetic**: Minimal per sample (dot product, comparisons)\n- **Loops**: Fixed iteration count (bounded by radius)\n- **Memory bandwidth**: Dominated by texture reads\n\n### Benchmarks (estimated)\n\n| Radius | Resolution | Expected FPS (RTX 4060) | Texture Samples/Frame |\n|--------|------------|------------------------|----------------------|\n| 10 | 1920×1080 | 150–200 FPS | ~40M |\n| 50 | 1920×1080 | 60–90 FPS | ~200M |\n| 100 | 1920×1080 | 30–45 FPS | ~400M |\n\nThe effect is **compute-bound** for large radii; performance scales inversely with radius.\n\n### Optimization Strategies\n\n1. **Early termination**: Break loop when perfect match found (brightness = 1.0)\n2. **MIP sampling**: For large radii, sample lower-resolution MIP levels\n3. **Compute shader**: For very large radii, use compute pass instead of fragment shader\n4. **Adaptive radius**: Reduce radius in dark areas where sorting has less visual impact\n\n---\n\n## Test Plan\n\n### Unit Tests (Minimum 80% Coverage)\n\n**Test 1: Parameter Defaults**  \n```python\ndef test_default_parameters():\n    effect = PixelSortEffect()\n    assert effect.params["radius'], ['threshold_low'], ['threshold_high'], ['direction'], ['feedback_amount'], ['threshold_low'], ['threshold_high'], ['radius'], ['direction'], ['threshold_low'], ['threshold_high'], ['threshold_low'], ['threshold_high'], ['u_mix'], ['u_mix'], ['radius'], ['x] Spec reviewed (by Manager or User before code starts)\n- [ ] All tests listed above pass\n- [ ] No file over 750 lines\n- [ ] No stubs in code\n- [ ] Verification checkpoint box checked\n- [ ] Git commit with `[Phase-3] P3-EXT127: PixelSortEffect` message\n- [ ] BOARD.md updated\n- [ ] Lock released\n- [ ] AGENT_SYNC.md handoff note written\n\n---\n\n## LEGACY CODE REFERENCES\n\n### VJlive (Original) Implementation\n\n**File:** `core/effects/legacy_trash/datamosh.py` (lines 321–340)  \n**Registration:** `core/effects/__init__.py` (datamosh category)  \n**Class:** `PixelSortEffect(Effect)`\n\nThe original GLSL fragment shader snippet:\n\n```glsl\nif (sampleBright >= threshold_low && sampleBright <= threshold_high) {\n    if (sampleBright < resultBright) {  // Note: < suggests it was finding MINIMUM\n        result = sample_color;\n        resultBright = sampleBright;\n    }\n}\n// ...\nfragColor = mix(current, result, clamp(mix + feedback_amount, 0.0, 1.0));\n```\n\nAnd the Python parameter initialization:\n\n```python\nclass PixelSortEffect(Effect):\n    "', 'Pixel sort - sorts pixels horizontally by brightness."', '\n\n    def __init__(self):\n        super().__init__("pixelsort', 'PIXEL_SORT_FRAGMENT)\n        // parameters likely present but truncated in snippet\n```\n\n**Key observations:**\n- Legacy used `pixelsort` and `pixel_sort` as registration keys\n- Algorithm searches horizontal neighborhood for pixel within brightness thresholds\n- Selects best match (though legacy snippet shows `<` comparison, suggesting minimum selection)\n- Blends with `mix + feedback_amount` parameters\n\n### VJLive3 Adaptation\n\n- Convert GLSL to WGSL with proper loop syntax\n- Use `>` comparison for maximum selection (standard pixel sorting)\n- Add explicit `u_direction` parameter for search direction\n- Follow VJLive3 conventions: `u_` prefix, `set_uniform` dispatch\n- Ensure proper boundary handling with `u_resolution`\n\n### Related Effects\n\n- **P5-DM14** `PixelBloomEffect` — also datamosh category, brightness-based but with feedback\n- **P5-DM11** `DatamoshEffect` — motion-based pixel bleeding (more complex)\n- **P5-DM13** `MeltEffect` — vertical pixel dripping\n- **P7-VE65** `SlitScanEffect` — temporal pixel displacement\n\n---\n\n## Implementation Notes for Phase 3\n\n1. **Loop bounds**: In WGSL, loops must have statically determinable bounds. Use `for` with `radius` clamped to compile-time maximum.\n2. **Integer arithmetic**: Convert UV to pixel coordinates for precise neighborhood calculation.\n3. **Boundary handling**: Clamp search window to [0, resolution.x] to prevent out-of-bounds sampling.\n4. **Performance**: Consider limiting maximum radius to 100-200 pixels for real-time performance.\n5. **Direction parameter**: Implement as sign multiplier in loop increment (`for (var x = start; x <= end; x += direction)`).\n\n---\n\n**End of Spec**  \n✅ Ready for Phase 3 implementation']]