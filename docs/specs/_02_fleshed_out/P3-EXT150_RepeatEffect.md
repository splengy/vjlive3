[[0, 1], [0, 1], [0, 1], {'name': 'repeat', 'gpu_tier': 'NONE', 'input_type': 'video', 'output_type': 'video', 'description': 'Repeat/tile effect - subdivides UV coordinates for tiling', 'parameters': [{'name': 'repeatX', 'type': 'float', 'min': 1.0, 'max': 10.0, 'default': 2.2}, {'name': 'repeatY', 'type': 'float', 'min': 1.0, 'max': 10.0, 'default': 2.2}, {'name': 'offsetX', 'type': 'float', 'min': 0.0, 'max': 10.0, 'default': 0.0}, {'name': 'offsetY', 'type': 'float', 'min': 0.0, 'max': 10.0, 'default': 0.0}]}, {'repeatX': 2.2, 'repeatY': 2.2, 'offsetX': 0.0, 'offsetY': 0.0}, ['Dict] = None, dt: float = 0.016) -> None:\n        "', '\n        Process incoming frame and update internal state.\n        \n        Args:\n            frame: Input video frame (HxWx3 or HxWx4 uint8)\n            beat_data: Optional beat detection data\n            dt: Time delta in seconds\n        "', '\n        self._input_frame = frame\n        self._time += dt\n        \n        # Process beat data if provided\n        if beat_data and beat_data.get(\'is_beat\', False):\n            self._on_beat(beat_data)\n            \n    def render(self) -> np.ndarray:\n        "', '\n        Generate output frame by applying repeat tiling.\n        \n        Returns:\n            Processed video frame (HxWx3 uint8)\n        "', '\n        if self._input_frame is None:\n            return np.zeros((480, 640, 3), dtype=np.uint8)\n            \n        h, w = self._input_frame.shape[:2]\n        \n        # Map parameters\n        rx = 1.0 + self.parameters[\'repeatX\'] / 10.0 * 9.0\n        ry = 1.0 + self.parameters[\'repeatY\'] / 10.0 * 9.0\n        ox = self.parameters[\'offsetX\'] / 10.0\n        oy = self.parameters[\'offsetY\'] / 10.0\n        \n        # Create coordinate grids\n        y, x = np.mgrid[0:h, 0:w]\n        u = x / w\n        v = y / h\n        \n        # Apply transformation\n        u_tiled = (u * rx + ox) % 1.0\n        v_tiled = (v * ry + oy) % 1.0\n        \n        # Sample input texture\n        # Using simple nearest-neighbor for demonstration\n        # In practice, would use OpenGL texture sampling\n        map_x = (u_tiled * w).astype(np.float32)\n        map_y = (v_tiled * h).astype(np.float32)\n        \n        # Remap image (CPU fallback)\n        output = cv2.remap(\n            self._input_frame,\n            map_x,\n            map_y,\n            interpolation=cv2.INTER_LINEAR,\n            borderMode=cv2.BORDER_WRAP\n        )\n        \n        return output\n    \n    def _on_beat(self, beat_data: Dict):\n        "', 'Handle beat detection event."', '\n        confidence = beat_data.get(\'confidence\', 0.5)\n        \n        # On strong beat, increment offsets for stepping effect\n        if confidence > 0.7:\n            step_size = 0.1  # Step by 10% of tile\n            self.parameters[\'offsetX\'] = (self.parameters[\'offsetX\'] + step_size) % 10.0\n            self.parameters[\'offsetY\'] = (self.parameters[\'offsetY\'] + step_size) % 10.0\n            \n    def reset(self) -> None:\n        "', 'Reset effect state."', '\n        self._time = 0.0\n        self._prev_beat_time = 0.0\n        \n    @property\n    def is_beat_detected(self) -> bool:\n        "', 'Check if beat was recently detected."', '\n        return False  # Would need state tracking\n        \n    @property\n    def current_bpm(self) -> float:\n        "', 'Get current BPM setting."', '\n        return self.parameters.get(\'bpm\', 120.0)\n```\n\n---\n\n## Inputs and Outputs\n\n| Name | Type | Description | Constraints |\n|------|------|-------------|-------------|\n| `frame` | `np.ndarray` | Input video frame | Shape: (height, width, 3), dtype: uint8 |\n| `beat_data` | `dict` (optional) | Beat detection data | Keys: \'is_beat\' (bool), \'confidence\' (float), \'bpm\' (float) |\n| `dt` | `float` | Time delta | Seconds, typically 0.016-0.033 |\n\n**Output**: Processed video frame (HxWx3 uint8) with tiled/repeated pattern.\n\n---\n\n## Edge Cases and Error Handling\n\n### Parameter Edge Cases\n- **repeatX or repeatY = 1.0**: No tiling, output equals input (possibly with offset)\n- **repeatX or repeatY = 10.0**: Maximum tiling, 10×10 grid of tiny tiles\n- **offsetX or offsetY = 0**: Aligned to edge, no offset\n- **offsetX or offsetY = 10**: Equivalent to 0 (wraps to 0 after division)\n- **Non-integer repeat factors**: Creates uneven tile sizes, still works\n- **Extreme offsets**: Wraps seamlessly due to fract operation\n\n### Error Scenarios\n- **Missing input frame**: Return black frame or previous output\n- **Invalid frame size**: Raise ValueError for dimensions < 1×1 or > 8192×8192\n- **Memory allocation failure**: Raise MemoryError with clear message\n- **NaN in parameters**: Clamp to valid range or raise error\n- **Performance degradation**: If frame processing exceeds 16ms, use GPU shader\n\n### Internal Dependencies\n- **Base Effect Class**: Would inherit from VJLive3 Effect base\n- **NumPy**: For coordinate grid generation (CPU fallback)\n- **OpenCV**: For remap operation (CPU fallback)\n- **GPU**: Optional for shader-based implementation (recommended)\n\n### External Dependencies\n- **Audio Analysis Module**: Provides beat_data (optional but recommended)\n- **GPU**: Optional for hardware acceleration\n\n---\n\n## Mathematical Formulations\n\n### UV Transformation\n\n**Base transformation:**\n$$u\' = \text{fract}(u cdot r_x + o_x)$$\n$$v\' = \text{fract}(v cdot r_y + o_y)$$\n\nWhere:\n- $(u, v)$ = normalized input coordinates [0,1]\n- $r_x, r_y$ = repeat factors (mapped from 1.0-10.0)\n- $o_x, o_y$ = offsets (mapped from 0.0-1.0)\n- $\text{fract}(x) = x - lfloor x \rfloor$ (fractional part)\n\n**Parameter mapping:**\n$$r_x = 1.0 + frac{\text{repeatX}}{10.0} \times 9.0$$\n$$r_y = 1.0 + frac{\text{repeatY}}{10.0} \times 9.0$$\n$$o_x = frac{\text{offsetX}}{10.0}$$\n$$o_y = frac{\text{offsetY}}{10.0}$$\n\n### Coordinate Grid Generation\n\n**Pixel to UV:**\n$$u[i,j] = frac{j}{W}$$\n$$v[i,j] = frac{i}{H}$$\n\nWhere $W$ = width, $H$ = height.\n\n**Remapping:**\nFor each output pixel $(i,j)$:\n1. Compute $(u,v)$ from output coordinates\n2. Transform to $(u\',v\')$ using repeat/offset\n3. Sample input texture at $(u\' \times W, v\' \times H)$\n\n### Blending (Optional)\n\nIf mixing with original:\n$$I_{out} = (1 - alpha) cdot I_{original} + alpha cdot I_{tiled}$$\n\nWhere $alpha$ = u_mix parameter.\n\n---\n\n## Performance Characteristics\n\n### CPU Requirements\n- **Processing load**: 5-10% CPU for real-time 60fps at 1080p\n- **Memory usage**: ~20MB for coordinate grids and remap buffers\n- **Frame rate target**: 60fps with optimized remapping\n- **Latency**: <5ms for coordinate transformation\n- **Algorithm complexity**: O(H × W) for coordinate generation + O(H × W) for remap\n\n### GPU Requirements (if implemented)\n- **Processing load**: <1% GPU for real-time 60fps at 1080p\n- **Memory usage**: Minimal (just texture and uniforms)\n- **Frame rate target**: 60fps with trivial fragment shader\n- **Latency**: <1ms with hardware acceleration\n- **Shader complexity**: Very low (simple UV math)\n\n### Memory Calculation\nFor 1080p (1920×1080):\n- Coordinate grids (float32): 1920 × 1080 × 2 × 4 = 16.6 MB\n- Remap intermediate buffer: 1920 × 1080 × 3 = 6.2 MB (uint8)\n- **Total**: ~25 MB\n\n**Optimization**: Use float16 or fixed-point coordinates to reduce memory by 50%.\n\n### Optimization Strategies\n- **GPU shader**: Implement in fragment shader for best performance\n- **Integer arithmetic**: Use fixed-point for offset calculation on CPU\n- **Avoid remap**: Direct texture sampling in shader eliminates remap overhead\n- **Precompute**: Cache coordinate grids if resolution doesn\'t change\n- **Region of interest**: Only process changed regions if offsets animate slowly\n\n---\n\n## Test Plan\n\n### Unit Tests (Coverage: 90%)\n\n1. **Parameter validation**\n   - Default values correct (repeatX=2.2, repeatY=2.2, offsetX=0, offsetY=0)\n   - Clamp out-of-range parameters\n   - Parameter type checking\n\n2. **Parameter mapping**\n   - repeatX=0 maps to rx=1.0\n   - repeatX=10 maps to rx=10.0\n   - offsetX=0 maps to ox=0.0\n   - offsetX=10 maps to ox=1.0\n   - Linear mapping correct\n\n3. **UV transformation**\n   - fract() wraps correctly\n   - Offset shifts tile pattern\n   - Repeat factors scale tile count\n   - Edge cases at boundaries (u=0, u=1)\n\n4. **Coordinate grid generation**\n   - Grid dimensions match frame size\n   - u ranges [0,1], v ranges [0,1]\n   - No NaN or Inf values\n\n5. **Remap operation**\n   - OpenCV remap produces valid output\n   - BorderMode=BORDER_WRAP works correctly\n   - Interpolation methods produce expected results\n\n6. **Audio integration**\n   - Beat detection increments offsets\n   - Confidence threshold works\n   - Offsets wrap at 10.0\n\n7. **Edge cases**\n   - Zero-sized frame: raises error\n   - Single-pixel frame: handles gracefully\n   - Extremely large frame: memory error or downscale\n\n### Integration Tests (Coverage: 85%)\n\n8. **Full render pipeline**\n   - Given synthetic input, produce valid output\n   - Output dimensions match input\n   - No artifacts or seams at tile boundaries\n   - Offsets animate smoothly\n\n9. **Tiling correctness**\n   - repeatX=2 produces 2 tiles horizontally\n   - repeatY=3 produces 3 tiles vertically\n   - Combined repeatX=2, repeatY=3 = 2×3 grid\n   - Offset shifts tile positions correctly\n\n10. **Performance benchmarks**\n    - 60fps achievable at 1080p on reference hardware\n    - Memory usage <50MB\n    - Frame time <16ms consistently\n    - GPU shader <1ms if available\n\n11. **Comparison with legacy**\n    - Output matches VJLive2 reference implementation\n    - Parameter behavior identical\n    - Visual quality comparable\n\n### Visual Quality Tests (Coverage: 75%)\n\n12. **Tiling quality**\n    - No visible seams at tile boundaries\n    - Smooth interpolation between pixels\n    - Offset animation smooth and continuous\n    - High repeat factors don\'t cause aliasing\n\n13. **Audio synchronization**\n    - Beat-synced offset stepping works\n    - No jarring jumps in animation\n    - Smooth transitions between parameter changes\n\n---\n\n## Definition of Done\n\n### Technical Requirements\n- [ ] All 4 parameters implemented with correct ranges and mapping\n- [ ] UV transformation using fract() correct\n- [ ] CPU fallback using OpenCV remap working\n- [ ] GPU shader implementation (recommended)\n- [ ] Beat synchronization with audio modules\n- [ ] Real-time 60fps at 1080p on reference hardware\n- [ ] Memory usage <50MB\n- [ ] Edge cases handled gracefully\n\n### Quality Requirements\n- [ ] 80%+ test coverage achieved\n- [ ] All unit tests passing\n- [ ] Integration tests verify full pipeline\n- [ ] Performance benchmarks documented\n- [ ] Error handling robust with clear messages\n- [ ] Visual quality matches legacy implementation\n\n### Integration Requirements\n- [ ] Compatible with VJLive3 effect chain\n- [ ] Proper parameter exposure via set_parameter()\n- [ ] Beat data integration with audio analyzer\n- [ ] Resource cleanup in reset()/delete()\n- [ ] Works alongside other video effects\n\n---\n\n## Legacy References\n\n### Original Implementation\n**Source**: `core/effects/legacy_trash/geometry.py` (VJlive Original)\n\n```python\nclass RepeatEffect(Effect):\n    "', 'Repeat/tile effect."', '\n\n    def __init__(self):\n        super().__init__("repeat', 'REPEAT_FRAGMENT)\n        self.parameters = {\n            "repeatX', 2.2, 'repeatY', 2.2, 'offsetX', 0.0, 'offsetY', 0.0], {'legacy': 'Simple UV coordinate manipulation\n- Uses `fract()` for seamless tiling\n- Parameters in 0-10 range with specific remapping\n- Mixes with original input via u_mix uniform\n- Very lightweight GPU effect\n\n### Related Specs\n- P3-EXT143 (R16VortexEffect): Similar UV manipulation\n- P3-EXT144 (RGBShiftEffect): Simple coordinate offset\n- P3-EXT156 (RotateEffect): Another geometry transformation\n\n### Shader Reference (WGSL for VJLive3)\n\n**Vertex shader (pass-through):**\n```wgsl\nstruct VertexOutput {\n    @builtin(position) position: vec4<f32>', 'uv': 'vec2<f32>'}, {'output': 'VertexOutput;\n    output.position = vec4<f32>(position', 'effect)': 'wgsl\nstruct FragmentInput {\n    @location(0) uv: vec2<f32>'}, {'repeatX': 'f32 = 2.2;      // 0-10 -> remapped\n    let repeatY: f32 = 2.2;\n    let offsetX: f32 = 0.0;\n    let offsetY: f32 = 0.0;\n    \n    // Remap to internal ranges\n    let rx = 1.0 + repeatX / 10.0 * 9.0;  // 1-10\n    let ry = 1.0 + repeatY / 10.0 * 9.0;  // 1-10\n    let ox = offsetX / 10.0;               // 0-1\n    let oy = offsetY / 10.0;               // 0-1\n    \n    // Transform UV\n    let uv_tiled = fract(input.uv * vec2<f32>(rx', 'Note**': 'The legacy shader uses `u_mix` to blend between original and tiled output. This parameter should be exposed in VJLive3 for flexibility.\n\n---\n\n## Easter Egg Idea\n\n**Secret Feature**:', 'Mode': 'Hold Ctrl+Alt+Shift+I to enable infinite recursion\n- The repeat effect recursively applies itself to its own output\n- Visual: Tiles within tiles within tiles', 'message': 'At repeatX=10', 'INFINITE': 'n the tile boundaries when viewed at 4K resolution\n- Easter egg: After 60 seconds of recursion', 'notes': 'Use `fract()` for seamless tiling (wraps coordinates automatically)\n- Implement in WGSL fragment shader for best performance\n- Provide CPU fallback using OpenCV remap for systems without GPU\n- Expose blend parameter to mix with original input\n- Consider adding rotation parameter for more creative possibilities (future enhancement)\n- Ensure proper handling of non-power-of-two textures (if applicable)\n\n**Next steps after spec approval:**\n1. Implement WGSL fragment shader with repeat/offset logic\n2. Add CPU fallback using NumPy + OpenCV\n3. Integrate with VJLive3 effect chain\n4. Add beat-synchronized offset stepping\n5. Create preset library (grid', 'expectations': 'GPU: <1ms per frame (essentially free)\n- CPU: 5-10ms per frame (acceptable but not ideal)\n- Memory: Minimal (no buffers needed)\n\nThis effect is so lightweight it can be chained multiple times without performance concerns'}]