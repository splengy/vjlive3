# Virtual environments only — everything else is visible
.venv/
venv/
ENV/
env/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
*.pyc

# Large binary/data files — too big for GitHub
WORKSPACE/MEMORY_BANK/QDRANT_FULL_BACKUP.json
agent-heartbeat/logs/
mcp_servers/vjlive3brain/brain.db

# Secrets — never commit regardless
config/secrets/
secrets/
*.secret
*.password
*_secret.txt
*_password.txt
