Task P3-VD01 Complete.

The `DepthLoopInjectionDatamosh` effect class (`depth_loop_injection.py`) and its test suite (`test_depth_loop_injection.py`) were both already implemented in the `vjlive3/plugins` directory.

I ran the test suite, achieving 86% test coverage, which exceeds our 80% requirement. No further code changes are necessary for this module since it already mirrors the requirements in the specification perfectly.

Worker Alpha signing off block.
